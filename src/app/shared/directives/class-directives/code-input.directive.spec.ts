import { CodeInputDirective } from './code-input.directive';

describe('CodeInputDirective', () => {
  it('should create an instance', () => {
    const directive = new CodeInputDirective();
    expect(directive).toBeTruthy();
  });
});
